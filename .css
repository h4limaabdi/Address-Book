body {
  font-family: Arial, sans-serif;
  max-width: 400px;
  max-height: 100vh;
  margin: 40px auto;
  padding: 20px;
}

form {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

#output {
  margin-top: 20px;
  border-top: 1px solid #ccc;
  padding-top: 10px;
}